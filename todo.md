


- add a "refrsh map button" in case someoneelse update teh map, to get latest map
- show only applicable pickable based on the graph of connected toll plaza (eg. cannot pick Plaza i as exit if there is no path from entrance plaza to Plaza i, and vice versa). when pick a plaza, either entrance or exit..., in dropdown, show only "reachable" plaza, in map, grey out non-reachable plazas 
- new page: just list each "line" and also show the "graph"
- name/logo "CarToll"  or "ค่าโทลล์"